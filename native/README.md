## React Native (Expo)

This folder is a separate Expo app.

### Requirements

- Node.js (LTS) + npm
- Expo Go app on your phone (optional) or Android Studio/Xcode

### Run

```bash
cd native
npm install
npx expo start
```

Or from repo root:

```bash
npm run dev:native
```
